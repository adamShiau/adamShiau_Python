#20240605
1.修改GUI介面字型顯示的亂碼問題。(需要都設定字型，若沒設定容易會有亂碼產生)

#20240527
1.修改儲存檔案時出現的bug。
	(1) 這一塊是因為之前的為了避免parameter被修改，而未儲存前一份的參數，所以每當使用儲存功能，都會先下dump撈取參數，點擊read就會將參數寫入txt檔案中。
	(2)為了避免在下dump時因為沒有取得參數值而出錯，因此在撈取參數轉換為json格式的時候出現eroor，所以有設定try-except的方式取得錯誤，並且不跳出error。
2.建置save要下dump的軸向，可以使用下拉方式選取，有分全部(三軸)、Z軸、X軸、Y軸，會根據選取的軸下dump。

#20240513
1.將parameter DAC_Gain的bug進行修正，以便將參數寫進儀器中。
2.修正溫度的小數點要顯示幾位數的部分。

# 20240417
1.將channel的標籤名稱改為Z、X、Y，Z=1、X=2、X=3。
2.修改SF0、SF1控制項的標籤名稱，SF0=slope、SF1=offset。
3.SF0、SF1與SFB的部份，若值太小之前顯示會以科學符號顯示，這次新增的功能是將科學符號轉成標準表示法的顯示方式。
4.將SF0、SF1的值在填寫時，是根據AA的方式會乘以10000，再顯示於控制項中。

# 20240416
1.將parameter的SF，只保留SF1、SF0，SF2~SF9、SFB、Tmin與Tmax的值都拿掉，再新增T1、T2、Slope1~3與offset1~3的控制項。(註:dump時可以將值撈取並顯示於介面)
2.當下dump之後，再修改任一控制項數值，會將該控制項的背景顯示為黃色，代表有被修改過。
3.當需要更新dump的值，可以點擊update按鈕進行更新，但點擊之後會跳出詢問視窗，確認是否要進行參數更新，選擇yes代表要更新，選擇no則是不要更新。
4.GUI已連接(connect)時，勾選save並read撈取數據前，會將channel1~3的parameter都寫進txt檔案中，參數最前面會加#字與數據進行區隔。

# 20240414之前
1.自動重連的部分，當產生reset時，接續寫入的數據不要寫入title。
2.新增透過輸入兩個值去計算DAC GAIN的數值功能。
3.新增角速度與加速度的校正參數設定介面功能。
4.修改卡爾曼功能，將六軸數據都進行過濾處理。